{
  "name": "group-chat-archive",
  "version": "0.1.0",
  "private": true,
  "description": "Offline browser for Twitter/X group DM exports.",
  "scripts": {
    "start": "node scripts/server.js",
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
