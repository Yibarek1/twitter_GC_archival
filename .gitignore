# ============================================================================
#  PRIVATE DATA — never commit. This repo ships only synthetic sample data.
# ============================================================================

# Built archive (real messages) — regenerate with `node build.js`
data.js

# Raw Twitter/X exports (full content + headers, 1:1 + group)
direct-messages.js
direct-messages-group.js
direct-message-headers.js
direct-message-group-headers.js
direct-messages*.js
direct-message-*.js

# Media folders (real photos & videos)
direct_messages_media/
direct_messages_group_media/
profile_pictures/
gc_photo.jpg

# Private local overrides (real names / profile pictures)
names.local.js

# Big packaged archive (multi-GB) — never commit
THE_GC.zip
THE_GC_V2.zip
*.zip

# Working areas
exports/
trash/
node_modules/

# Internal planning brief (references real ids / private data)
PLAN.md

# Scraper capture artifacts
gc-scrape-*.json
sender-map.json

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# ============================================================================
#  Explicitly KEEP the synthetic, publishable sample (negate the broad ignore)
# ============================================================================
!data.sample.js
!sample_media/
